---
- name: "AWS SSM 기반 무SSH 프로비저닝"
  hosts: all
  become: true
  gather_facts: true
  vars:
    lecture_id: "lecture19"
    lecture_title: "AWS SSM 기반 무SSH 프로비저닝"
    install_enabled: false
    reference_playbook: "../../playbooks/20_aws_provision_ssm_no_ssh.yml"
    lecture_packages:
      - python3
      - python3-boto3
      - python3-botocore
      - awscli
      - jq
  tasks:
    - name: Show lecture context
      ansible.builtin.debug:
        msg:
          - "id={{ lecture_id }}"
          - "title={{ lecture_title }}"
          - "reference_playbook={{ reference_playbook }}"
          - "install_enabled={{ install_enabled }}"

    - name: Install lecture packages when enabled
      ansible.builtin.package:
        name: "{{ lecture_packages }}"
        state: present
      when: install_enabled | bool

    - name: Check Python3 availability
      ansible.builtin.command: python3 --version
      register: python_version
      changed_when: false
      failed_when: false

    - name: Print Python3 version
      ansible.builtin.debug:
        var: python_version.stdout
